import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicLoginForm',
  appName: 'LoginForm',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
